
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
bin/
.beads/*
/bento
/coverage.out
dist/
.cover/

# The Quick Start builds the CLI next to the script it sandboxes.
/examples/probe/bento

# `go build .` in an example module names the binary after the module.
/examples/embed/bentoembed
/examples/supervise/bentosupervise

